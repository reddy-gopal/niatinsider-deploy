/**
 * @deprecated Footer is injected from the app via ReviewsUiProvider `footer` prop.
 * Use `@/components/reviews/ReviewsFooter` (wraps shared InsiderFooter).
 */
export function Footer() {
  return null;
}
