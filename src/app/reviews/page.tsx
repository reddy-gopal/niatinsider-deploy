'use client';

import { ProspectiveHomePage } from '@niat/reviews-ui/pages/ProspectiveHomePage';

/** Intermediate / NIAT students: niatreviews home (search + FAQ). Seniors redirect in-page. */
export default function ReviewsPage() {
  return <ProspectiveHomePage />;
}
