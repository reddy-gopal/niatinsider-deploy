'use client';

import InsiderFooter from '@/components/InsiderFooter';

/** Reviews shell footer — same component and role-based menus as the main site. */
export default function ReviewsFooter() {
  return <InsiderFooter reviewsBasePath="/reviews" />;
}
